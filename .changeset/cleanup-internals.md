---
"bridgerton": patch
---

Remove dead code from client.ts (unused `promptForKey`/`ensureApiKey`), replace sloppy `as any` cast with proper Format type, and use Node shebang for cross-runtime support.
